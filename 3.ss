(define nearest-point
  (lambda (p list-of-points)
    
(define union
  (lambda (s1 s2)
    
(define intersection
  (lambda (s1 s2)
    
(define subset?
  (lambda (s1 s2)
    
(define relation?
  (lambda (obj)
    
(define domain
  (lambda (rel)
    
(define reflexive?
  (lambda (rel)
    
(define hailstone-step-count 
  (lambda (n)